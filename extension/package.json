{
  "name": "heli-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "One-click capture into Heli. Built separately from the app — see README.md.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "package": "node scripts/build.mjs --package",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
